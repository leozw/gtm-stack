# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignorar diretório .terraform onde os plugins são armazenados
**/.terraform/*

# Ignorar arquivos de estado local que podem conter informações sensíveis
*.tfstate
*.tfstate.*.backup
*.tfstate.backup

# Ignorar arquivos de configuração do plano que podem conter informações sensíveis
*.tfplan
*.tfplan.*

# Ignorar arquivos de log
*.tflog

# Ignorar arquivos de variáveis de ambiente que podem conter informações sensíveis
*.auto.tfvars
*.auto.tfvars.json

# Ignorar arquivos de override, que são geralmente específicos para o usuário
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignorar arquivos de configuração CLI que podem conter informações sensíveis
.terraformrc
terraform.rc